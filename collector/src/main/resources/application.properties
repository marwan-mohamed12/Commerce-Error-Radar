# Collector HTTP
server.port=8088

spring.application.name=commerce-error-radar

# SQLite
spring.datasource.url=jdbc:sqlite:${radar.sqlite-path}
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.hikari.maximum-pool-size=1
spring.sql.init.mode=never

# --- Radar ---
# Hybris home = folder that contains hybris\log\tomcat\console-*.log
# Override with env HYBRIS_HOME or --radar.hybris-home=D:\hybris
radar.hybris-home=${hybris.home:${HYBRIS_HOME:}}

# First stack frame with this prefix becomes the fingerprint
radar.custom-package-prefix=${RADAR_PREFIX:com.yourcompany}

# true = tail from end (live). false = replay the whole current file
radar.tail-from-end=true
radar.poll-interval-ms=500
radar.context-lines=30
radar.ring-buffer-size=100
radar.sqlite-path=${user.dir}/data/radar.db
radar.sample-logs-dir=${user.dir}/../sample-logs

# Drop events whose own text contains any of these (comma-separated)
radar.ignore-patterns=Solr ping,session replication,HAC login,actuator/health

logging.pattern.console=%d{HH:mm:ss.SSS} %-5level [%logger{20}] %msg%n
logging.level.com.commerce.radar=INFO
logging.level.org.springframework.jdbc=WARN
