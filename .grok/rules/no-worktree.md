# No worktrees

This project’s only working copy is **`F:\grok\Commerce-Error-Radar`**.

- Edit files there. Never under `~/.grok/worktrees/`.
- Do not use `isolation: "worktree"`, `grok worktree`, or `git worktree add` for this repo.
- If you are already in a grok worktree, switch to the canonical checkout before writing anything.
